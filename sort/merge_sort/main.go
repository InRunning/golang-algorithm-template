package merge_sort

//func MergeSort(nums []int) {
//	for currentSize := 1; currentSize <= len(nums)-1; currentSize *= 2 {
//		for leftStart := 0; leftStart < len(nums)-1; leftStart += 2 * currentSize {
//			mid := common.Min(leftStart+currentSize-1, len(nums)-1)
//			rightEnd := common.Min(leftStart+2*currentSize-1, len(nums)-1)
//			merge(nums, leftStart, mid, rightEnd)
//		}
//	}
//}
//
//func merge(nums []int, leftStart int, mid int, rightEnd int) {
//	left := make([]int)
//}
//
//func min()
